// function mathPower(num, power) {

//     let sum = 1;
//     sum = num ** power;
//     // sum = Math.pow(num, power)
//     return sum;

// }

let mathPower = (num, power) => {
    return Math.pow(num, power)
}
console.log(mathPower(2, 8));